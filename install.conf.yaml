- clean: ['~']

- link:
    ~/.bashrcs: bashrcs/
    ~/.ctags: ctags
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.gnuplot: gnuplot
    ~/.gnuplotd: gnuplotd/
    ~/.scripts: scripts/
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.vmdrc: vmdrc
    ~/.wgetrc: wgetrc
    ~/.zshrc: zshrc
    ~/.zprofile: zprofile
    ~/.conda-env-defaults.txt: conda-env-defaults.txt
    ~/.latexmkrc: latexmkrc
    #~/.pixi: pixi  # TODO: get the right things
    #~/Library/texmf: texmf/ # on a Mac, at least

